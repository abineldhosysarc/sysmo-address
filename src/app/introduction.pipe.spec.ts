import { IntroductionPipe } from './introduction.pipe';

describe('IntroductionPipe', () => {
  it('create an instance', () => {
    const pipe = new IntroductionPipe();
    expect(pipe).toBeTruthy();
  });
});
